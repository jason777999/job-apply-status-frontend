import React from "react";

export default function Note() {
  return (
    <div
      style={{
        width: "200px",
        background: "gray",
        height: "100%",
        display: "flex",
        flexDirection: "column",
      }}
    >
      Hello I am note
    </div>
  );
}
